{
  "name": "rss-ai-augmented",
  "version": "1.0.0",
  "description": "RSS reader with AI-powered sorting",
  "scripts": {
    "start": "npx pm2 start ecosystem.config.js",
    "stop": "npx pm2 stop ecosystem.config.js",
    "restart": "npx pm2 restart ecosystem.config.js",
    "reload": "npx pm2 reload ecosystem.config.js",
    "delete": "npx pm2 delete ecosystem.config.js",
    "logs": "npx pm2 logs rss-ai-augmented",
    "status": "npx pm2 status",
    "dev": "npx pm2 start ecosystem.config.js --env development"
  },
  "devDependencies": {
    "pm2": "^5.4.2"
  }
}
